import React from 'react';
import { NODE } from 'data';

export default {
  id: 'grab-files',
  name: 'grab-files',
  technologies: [ NODE ],
  github: 'https://github.com/kamilmielnik/grab-files',
  npm: 'https://www.npmjs.com/package/grab-files',
  summary: (
    <p>
      If you have a text file with a list of URLs pointing to files, this tool will download them for you.
    </p>
  )
};
